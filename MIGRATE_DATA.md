# 📦 Миграция данных из SQLite в Supabase

## Важно!

⚠️ **Перед миграцией:**
1. Убедитесь, что приложение **НЕ запущено**
2. Создайте резервную копию локальной базы данных (на всякий случай)
3. Убедитесь, что схема в Supabase уже создана (выполнили SQL миграцию)

---

## Шаг 1: Установить зависимости

Скрипт использует `better-sqlite3` (уже должен быть установлен) и `@supabase/supabase-js` (уже установлен).

Если нет - установите:
```bash
npm install better-sqlite3 @supabase/supabase-js
```

---

## Шаг 2: Подготовить переменные окружения

Создайте файл `.env.local` в корне проекта (или используйте переменные окружения):

```bash
VITE_SUPABASE_URL=https://bddgzxvhosxlyildlmya.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_y6GXqTFLQ9NvMoOpOeHm4A_osnntYOX
```

**Важно:** Не коммитьте `.env.local` в git (он уже в .gitignore)

---

## Шаг 3: Запустить миграцию

```bash
node scripts/migrate-to-supabase.mjs
```

Скрипт:
1. ✅ Найдет вашу локальную SQLite базу
2. ✅ Загрузит все данные
3. ✅ Преобразует JSON поля
4. ✅ Загрузит в Supabase

---

## Что мигрируется:

- ✅ Tasks (задачи)
- ✅ Customers (клиенты)
- ✅ Contractors (подрядчики)
- ✅ Goals (цели)
- ✅ Monthly Financial Goals (месячные финансовые цели)
- ✅ Credits + Schedules (кредиты и графики платежей)
- ✅ Incomes (доходы)
- ✅ Settings (настройки)
- ✅ Calculations (расчеты)
- ✅ Tax Paid Flags (флаги оплаты налогов)
- ✅ Extra Work (дополнительная работа)

---

## После миграции:

1. Проверьте данные в Supabase:
   - Откройте Supabase Dashboard → Table Editor
   - Проверьте, что данные появились в таблицах

2. Откройте приложение в браузере:
   - https://crm-desktop-84es.vercel.app/
   - Данные должны загружаться из Supabase

---

## Если что-то пошло не так:

1. **Ошибка "Database not found"**:
   - Убедитесь, что приложение запускалось хотя бы раз
   - Проверьте путь к базе в скрипте

2. **Ошибка подключения к Supabase**:
   - Проверьте ключи в переменных окружения
   - Проверьте, что проект Supabase активен

3. **Ошибка при вставке данных**:
   - Убедитесь, что схема БД создана (выполнили SQL миграцию)
   - Проверьте логи в консоли

---

## Безопасность:

⚠️ **Не публикуйте ключи Supabase!**  
`.env.local` уже в `.gitignore`, но убедитесь, что не коммитите ключи в код.
