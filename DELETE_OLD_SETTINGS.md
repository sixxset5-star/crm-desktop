# 🗑️ Удаление старых записей настроек

## Выполните этот SQL в Supabase SQL Editor:

```sql
-- Удалить все старые записи настроек кроме 'main'
DELETE FROM settings WHERE key != 'main';
```

---

## Проверка:

После удаления выполните:

```sql
SELECT key FROM settings;
```

Должна быть только одна запись с `key='main'`.

---

## После этого:

1. Обновите страницу в браузере с очисткой кэша (`Ctrl+Shift+R` или `Cmd+Shift+R`)
2. Праздники и выходные должны появиться
