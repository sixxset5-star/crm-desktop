# ✅ Код готов к загрузке!

Remote уже настроен: `https://github.com/sixxset5-star/crm-desktop.git`

## 🚀 Выполните одну команду:

```bash
git push -u origin main
```

Если попросит пароль, используйте **Personal Access Token** (не обычный пароль):

### Как получить токен:

1. Откройте https://github.com/settings/tokens
2. Generate new token → Generate new token (classic)
3. Выберите срок действия (например, 90 days)
4. Отметьте scope: `repo` (полный доступ к репозиториям)
5. Generate token
6. **Скопируйте токен** (он показывается только один раз!)
7. При запросе пароля введите этот токен

### Альтернатива: Настроить SSH (рекомендуется)

```bash
# 1. Проверить есть ли SSH ключ
ls -la ~/.ssh/id_rsa.pub

# 2. Если нет - создать
ssh-keygen -t ed25519 -C "your_email@example.com"

# 3. Добавить ключ в GitHub
cat ~/.ssh/id_rsa.pub
# Скопируйте вывод и добавьте на https://github.com/settings/keys

# 4. Переключить remote на SSH
git remote set-url origin git@github.com:sixxset5-star/crm-desktop.git

# 5. Запушить
git push -u origin main
```

---

**После успешного push:**

Настройте Vercel: https://vercel.com/new → Import Git Repository → `crm-desktop`

