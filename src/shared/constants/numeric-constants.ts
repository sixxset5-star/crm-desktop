/**
 * Общие числовые константы для всего приложения
 * Используются вместо магических чисел для улучшения читаемости и поддерживаемости кода
 */

// ============================================================================
// Временные константы
// ============================================================================

/**
 * Задержка для анимаций и таймаутов (в миллисекундах)
 * Используется для небольших задержек при загрузке данных или обновлении UI
 */
export const DELAY_MS = 100;

/**
 * Увеличенная задержка для обновления store (в миллисекундах)
 * Используется когда требуется больше времени для обновления состояния после загрузки данных
 */
export const DELAY_MS_LONG = 200;

/**
 * Задержка для debounce (в миллисекундах)
 * Используется для отложенного выполнения действий при вводе текста (например, поиск)
 */
export const DEBOUNCE_DELAY_MS = 300;

/**
 * Интервал обновления данных (в миллисекундах)
 * Используется для периодического обновления данных (например, напоминаний)
 */
export const UPDATE_INTERVAL_MS = 60000; // 1 минута

// ============================================================================
// Календарные константы
// ============================================================================

/**
 * Количество дней в неделе
 */
export const DAYS_IN_WEEK = 7;

/**
 * Количество дней в месяце (приблизительное значение для валидации)
 * Используется для ограничения ввода дней в формах
 */
export const DAYS_IN_MONTH = 30;

/**
 * Количество дней в году
 * Используется для ограничения ввода дней в формах (например, архивация)
 */
export const DAYS_IN_YEAR = 365;

/**
 * Количество месяцев в году
 * Используется для генерации списков месяцев и ограничения ввода
 */
export const MONTHS_IN_YEAR = 12;

/**
 * Количество месяцев вперед для отображения задач на доске
 * Используется для фильтрации задач по дедлайну
 */
export const MONTHS_AHEAD_FOR_TASKS = 3;

// ============================================================================
// Процентные константы
// ============================================================================

/**
 * Максимальное значение прогресса в процентах
 * Используется для прогресс-баров и слайдеров
 */
export const MAX_PROGRESS_PERCENT = 100;

/**
 * Минимальное значение прогресса в процентах
 * Используется для прогресс-баров и слайдеров
 */
export const MIN_PROGRESS_PERCENT = 0;

// ============================================================================
// Шаги и лимиты для форм
// ============================================================================

/**
 * Шаг для множителей в калькуляторе
 * Используется для полей ввода множителей (например, множитель для "фотки есть")
 */
export const MULTIPLIER_STEP = 0.1;

/**
 * Минимальное значение множителя
 * Используется для валидации множителей в калькуляторе
 */
export const MULTIPLIER_MIN = 0.1;

// ============================================================================
// Константы времени (часы, минуты, секунды)
// ============================================================================

/**
 * Количество часов в сутках
 */
export const HOURS_IN_DAY = 24;

/**
 * Количество минут в часе
 */
export const MINUTES_IN_HOUR = 60;

/**
 * Количество секунд в минуте
 */
export const SECONDS_IN_MINUTE = 60;

/**
 * Последний час дня (23:00)
 * Используется для установки времени конца дня
 */
export const HOUR_END_OF_DAY = 23;

/**
 * Последняя минута часа (59)
 * Используется для установки времени конца часа
 */
export const MINUTE_END_OF_HOUR = 59;

/**
 * Последняя секунда минуты (59)
 * Используется для установки времени конца минуты
 */
export const SECOND_END_OF_MINUTE = 59;

// ============================================================================
// Пороги виртуализации
// ============================================================================

/**
 * Порог количества элементов для виртуализации больших списков
 * Используется для клиентов, подрядчиков и других больших списков
 */
export const VIRTUALIZATION_THRESHOLD = 50;

/**
 * Порог количества задач для виртуализации на доске
 * Используется для колонок задач на доске
 */
export const VIRTUALIZATION_THRESHOLD_TASKS = 20;

// ============================================================================
// Размеры и размеры компонентов
// ============================================================================

/**
 * Минимальная высота контейнера (в пикселях)
 * Используется для виртуализированных списков и других контейнеров
 */
export const MIN_CONTAINER_HEIGHT = 600;

/**
 * Минимальная ширина колонки в grid (в пикселях)
 * Используется для виртуализированных grid списков
 */
export const GRID_MIN_COLUMN_WIDTH = 300;

/**
 * Высота карточки клиента (в пикселях)
 * Используется для виртуализированного grid клиентов
 */
export const CUSTOMER_CARD_HEIGHT = 280;

/**
 * Высота карточки подрядчика (в пикселях)
 * Используется для виртуализированного grid подрядчиков
 */
export const CONTRACTOR_CARD_HEIGHT = 300;

/**
 * Отступ от верха viewport для вычисления высоты контейнера (в пикселях)
 * Используется в формуле calc(100vh - VIEWPORT_OFFSET_FOR_HEIGHT)
 */
export const VIEWPORT_OFFSET_FOR_HEIGHT = 300;

// ============================================================================
// Константы событий
// ============================================================================

/**
 * Количество кликов для двойного клика
 * Используется для обработки событий двойного клика
 */
export const DOUBLE_CLICK_COUNT = 2;

