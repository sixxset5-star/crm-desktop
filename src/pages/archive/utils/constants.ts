/**
 * Константы для страницы Archive
 * Используются вместо хардкодных значений
 * Все значения основаны на дизайн-токенах из tokens.css
 */

// Размеры для компонентов Archive (используем CSS токены)
export const ARCHIVE_SELECT_MIN_WIDTH = 'var(--archive-select-min-width)';
export const ARCHIVE_CARD_GRID_MIN_WIDTH = 'var(--archive-card-grid-min-width)';
export const ARCHIVE_CARD_ICON_SIZE = 'var(--icon-size-sm)';

