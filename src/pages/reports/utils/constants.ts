/**
 * Константы для страницы Reports
 * Используются вместо хардкодных значений
 * Все значения основаны на дизайн-токенах из tokens.css
 */

// Размеры для компонентов Reports (используем CSS токены)
export const REPORTS_BAR_HEIGHT = 'var(--control-lg-height)';
export const REPORTS_BAR_PADDING_LEFT = 'var(--space-md)';
export const REPORTS_MONTH_LABEL_MIN_WIDTH = 'var(--reports-month-label-min-width)';
export const REPORTS_TASK_COUNT_MIN_WIDTH = 'var(--reports-task-count-min-width)';
export const REPORTS_MODAL_MAX_HEIGHT = 'var(--reports-modal-max-height)';

// Тени для текста (используем CSS токены)
export const REPORTS_TEXT_SHADOW = 'var(--text-shadow-sm)';

